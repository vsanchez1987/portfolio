import styles from "styled-components";

export const EventUlComponentStyled = styles.ul`
  position: absolute;
  top: 170px;
  padding: 0;
  list-style: none;
  white-space: nowrap;
  font-size: 0;
`;
